name: "Build"
description: "Build all the Docker compose containers"
runs:
  using: "composite"
  steps:
    - name: Set up Docker
      uses: docker/setup-docker@v3
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    - name: Start the containers
      shell: bash
      run: make start
